# Canvas 特效训练营 · 课程大纲

> 从画一个方块，到写一个星云着色器。
> 两大阶段：**Canvas 2D**（CPU 绘制 / 数学 / 动画 / 像素 / 物理）→ **WebGL**（GPU 管线 / GLSL / 噪声 / 星云）。
> 共 35 课，每课一个独立可运行的 HTML，由浅入深，最终汇入「音画黑洞」大作业。

---

## 阶段一 · 基础绘制（认识画布）

### 01 画布与坐标系
- **核心 API**：`canvas.width/height`、`getContext('2d')`、`fillRect`、`strokeRect`、`clearRect`
- **知识点**：canvas 元素与属性；坐标系原点在左上角、x 向右、y 向下；CSS 尺寸 ≠ 画布分辨率
- **产出**：认识坐标系，绘制第一个方块
- **挑战**：用 `requestAnimationFrame` 让方块动起来（动画课的预告）

### 02 路径与形状
- **核心 API**：`beginPath`、`moveTo`、`lineTo`、`closePath`、`arc`、`fill`、`stroke`
- **知识点**：路径的构建过程（开启 → 定义 → 闭合 → 填充/描边）；圆与圆弧的角度约定
- **产出**：多边形 + 时钟表盘
- **挑战**：画一个正 N 边形，观察边数变多趋近圆

### 03 样式与渐变色
- **核心 API**：`fillStyle`、`strokeStyle`、`lineWidth`、`createLinearGradient`、`createRadialGradient`
- **知识点**：颜色表示（`rgb/hsl`）；线性/径向渐变；渐变对象复用
- **产出**：渐变天空日落
- **挑战**：用 `hsl()` 生成彩虹色带

### 04 阴影与透明度
- **核心 API**：`shadowBlur`、`shadowColor`、`shadowOffsetX/Y`、`globalAlpha`、`save/restore`
- **知识点**：阴影四件套；透明度合成；状态栈的压栈/出栈
- **产出**：霓虹发光效果
- **挑战**：双层描边做出「光晕 + 描边」的招牌字

---

## 阶段二 · 图形数学（理解坐标与方程）

### 05 三角函数与圆形运动
- **核心 API**：`Math.sin`、`Math.cos`、`arc`
- **知识点**：单位圆与三角函数；极坐标 → 直角坐标；角度/弧度换算
- **产出**：旋转的圆点、呼吸的星空
- **挑战**：多个点沿圆轨道运动，构成「行星环」

### 06 参数方程曲线
- **核心 API**：`beginPath` + 数学推导、`lineTo`
- **知识点**：参数方程概念；心形曲线公式；通过采样点连线成型
- **产出**：**心形曲线**（对应原 `heart.html`）
- **挑战**：改参数画出玫瑰曲线 `r = a·sin(kθ)`

### 07 贝塞尔曲线
- **核心 API**：`quadraticCurveTo`、`bezierCurveTo`
- **知识点**：二次/三次贝塞尔；控制点与形状；用贝塞尔画波浪
- **产出**：波浪线 / 花瓣
- **挑战**：让控制点随动画移动，形成「流动的旗帜」

### 08 变换矩阵
- **核心 API**：`translate`、`rotate`、`scale`、`transform`、`save/restore`
- **知识点**：坐标系变换；变换的叠加顺序（后调用的先作用）；矩阵表达
- **产出**：旋转的花环
- **挑战**：递归缩放绘制「树状分形」

---

## 阶段三 · 动画与交互（动起来）

### 09 动画循环与时间
- **核心 API**：`requestAnimationFrame`、时间戳
- **知识点**：rAF 与 setTimeout 的区别；基于时间（毫秒）驱动而非帧数；`clearRect` 重绘
- **产出**：弹跳的小球
- **挑战**：用时间差值（deltaTime）保证不同刷新率下速度一致

### 10 缓动与物理
- **核心 API**：插值（`lerp`）、easing 函数、速度/加速度
- **知识点**：匀速 → 匀加速；重力；阻尼与反弹系数
- **产出**：抛物线抛物体
- **挑战**：加入空气阻力，观察轨迹变化

### 11 鼠标交互与碰撞
- **核心 API**：`mousemove`、`offsetX/offsetY`、碰撞检测
- **知识点**：坐标换算（canvas 相对偏移）；点-圆、圆-圆碰撞；边界反弹
- **产出**：跟随鼠标的小球
- **挑战**：多个小球互撞弹性碰撞

### 12 合成与混合模式
- **核心 API**：`globalCompositeOperation`、`clip`
- **知识点**：合成模式（`lighter`/`source-over`/`multiply`…）；裁剪区域；光晕叠加
- **产出**：光晕 / 遮罩挖洞
- **挑战**：用 `lighter` 做粒子叠加发光

---

## 阶段四 · 音频可视化（进阶：外部数据源）

### 13 Web Audio 频谱分析
- **核心 API**：`AudioContext`、`AnalyserNode`、`getByteFrequencyData`、`getUserMedia`
- **知识点**：音频流管线；FFT 与频率分箱；`Uint8Array` 数据读取；频率→图形高度映射
- **产出**：**基础频谱柱状图**（对应原 `index.html`）
- **挑战**：按对数分频段取平均值，画更平滑的柱状图

### 14 波形与声控动画
- **核心 API**：`getByteTimeDomainData`、`requestAnimationFrame`
- **知识点**：时域 vs 频域；波形采样点连线
- **产出**：波形线 / 跳动的心
- **挑战**：把波形采样点作为粒子坐标，让波形「开花」

### 15 音频驱动视觉
- **核心 API**：频率 → 参数映射（力度、颜色、半径）
- **知识点**：数据归一化；参数平滑（阻尼滤波，避免抖动）；音画联动设计
- **产出**：声控粒子爆发
- **挑战**：低频/中频/高频分别驱动不同视觉元素

---

## 阶段五 · 粒子与特效（高频视觉）

### 16 粒子系统
- **核心 API**：对象数组、`requestAnimationFrame`、对象池思想
- **知识点**：粒子生命周期（出生/运动/消亡）；批量更新与绘制；性能优化（限制数量）
- **产出**：粒子星空 / 雪花
- **挑战**：粒子带颜色渐变与透明衰减

### 17 鼠标交互粒子
- **核心 API**：鼠标位置、力场计算
- **知识点**：粒子与鼠标的距离/方向；排斥或吸引；平滑跟随
- **产出**：跟随鼠标的粒子流
- **挑战**：鼠标扫过留下粒子「拖尾」

### 18 文字粒子
- **核心 API**：`drawImage`（离屏 canvas）、`getImageData`
- **知识点**：离屏 canvas 渲染文字；采样像素坐标；把「字」变成「点阵粒子」
- **产出**：文字消散成粒子
- **挑战**：多段文字之间切换的粒子形变动画

---

## 阶段六 · 像素级图像处理（高阶 API）

### 19 像素操作
- **核心 API**：`getImageData`、`putImageData`、`createImageData`
- **知识点**：RGBA 通道与像素遍历；性能注意点
- **产出**：马赛克 / 反色 / 灰度
- **挑战**：局部放大「鱼眼」效果

### 20 卷积滤镜
- **核心 API**：卷积核（矩阵）数学
- **知识点**：卷积原理；边缘处理；常见核（模糊/锐化/浮雕/描边）
- **产出**：模糊、浮雕、锐化滤镜
- **挑战**：滑块实时调节模糊半径

### 21 视频/图片特效
- **核心 API**：`drawImage`（视频帧）+ 像素循环
- **知识点**：把 `<video>` 当图片源逐帧绘制；对帧做像素特效；性能与降采样
- **产出**：视频水波纹 / 像素风
- **挑战**：摄像头画面实时特效滤镜

---

## 阶段七 · 物理与流体（毕业级）

### 22 弹簧与摆
- **核心 API**：力学模拟、阻尼、欧拉积分
- **知识点**：胡克定律；谐振；粒子连线（布/绳）
- **产出**：果冻 / 布料
- **挑战**：N 个质点 + 约束迭代模拟布料下垂

### 23 透明折射球
- **知识点**：透明球 = 折射（clip 内放大背景）+ 高光 + 边缘暗环 + 阴影；多层叠加的视觉欺骗
- **产出**：**可拖动的透明折射球**
- **挑战**：调出明显的折射效果，网格线在球内被放大

### 24 综合大作业（Canvas 2D 篇）
- **知识点**：整合前段全部 Canvas 2D 技能（渐变/粒子/鼠标力场/音频节拍/发光拖尾）
- **产出**：音画粒子宇宙 —— 螺旋星系随音乐呼吸，低频掀起冲击波，鼠标扰动粒子
- **挑战**：加入歌词/谱面字幕

---

## 阶段八 · WebGL 入门（25 全局观 + 26-29 动手）（认识 GPU 渲染管线）

### 25 WebGL 全局观
- **核心知识**：GPU 渲染管线总览（CPU 顶点数据 → 顶点缓冲 → 顶点着色器 → 光栅化 → 片段着色器 → 屏幕）
- **知识点**：顶点着色器（每顶点执行） vs 片段着色器（每像素执行）；交互式管线动画图
- **产出**：**可步进/自动播放的渲染管线图**
- **挑战**：用阶段滑块走完整个管线

### 26 WebGL 初体验
- **核心知识**：渲染管线（顶点 → 光栅化 → 片元）；CPU vs GPU；`getContext('webgl')`
- **知识点**：编译着色器；绑定缓冲；`drawArrays`
- **产出**：用 WebGL 画一个红色三角形
- **挑战**：对比 2D canvas 画同一图形，感受两者心智模型差异

### 27 GLSL 语言基础
- **核心知识**：数据类型（`float/int/bool/vec2~4/mat2~4`）；`in/out/uniform`；内置函数（`mix/clamp/smoothstep/fract`）
- **产出**：渐变彩色三角形
- **挑战**：用 `smoothstep` 画硬边圆

### 28 顶点缓冲与矩阵
- **核心知识**：`attribute` 变量、`createBuffer`、`vertexAttribPointer`、变换矩阵
- **知识点**：在 CPU 端用矩阵变换顶点；模型/视图/投影概念
- **产出**：旋转的矩形
- **挑战**：画一个旋转立方体（线框）

### 29 纹理与 UV
- **核心知识**：纹理单元、UV 坐标、图像上传与采样
- **知识点**：`createTexture`、`texImage2D`、`texture2D` 采样
- **产出**：旋转的贴图方块
- **挑战**：UV 空间里做几何扭曲

---

## 阶段九 · Shader 特效修炼（片段着色器）

### 30 UV 与 SDF 距离场
- **核心知识**：UV 归一化；`length`、`smoothstep`；SDF（有向距离场）
- **知识点**：圆环/心形等隐式方程；用距离函数做抗锯齿（AA）
- **产出**：圆环 / 心形 SDF
- **挑战**：SDF 叠加与布尔运算（并集/差集）

### 31 噪声与 FBM
- **核心知识**：`hash` 函数、value noise、Perlin 噪声、fBm（分形布朗运动）
- **知识点**：伪随机与平滑插值；多层噪声叠加；域扭曲（domain warping）
- **产出**：噪点云朵纹理
- **挑战**：把 fBm 结果映射成地形高度（伪 3D 效果）

### 32 时间动画与分层
- **核心知识**：`uniform` 时间、`mat2` 旋转、循环叠加图层
- **知识点**：用时间驱动位移/旋转；for 循环分层叠加视差
- **产出**：流动的星空
- **挑战**：旋转整个 UV 空间制造「银河旋臂」

---

## 阶段十 · 星云大作业（拆解重写 master 分支）

### 33 星云着色器解剖
- **核心知识**：逐行拆解 `starField.effect`（master 分支）
- **知识点**：`fbmslow`（fBm 星云底）、`Star()`（十字星芒 + 旋转）、`StarLayer()`（哈希网格定位星星）、`smoothstep` 渐隐、多层视差
- **产出**：在原生 WebGL 里完整复刻星野
- **挑战**：调整星芒数量、颜色分布，做出自己的风格

### 34 音频驱动 Shader
- **核心知识**：把音频频率写入 `uniform`（复用 13 课的 Web Audio 代码）
- **知识点**：CPU（JS 分析音频）→ GPU（shader 采样）的数据通路；归一化与平滑
- **产出**：声控星云（音量推高星云浓度）
- **挑战**：低频驱动星星大小、高频驱动星云亮度，做三频联动

### 35 综合大作业（终极）
- **知识点**：音频星云 + 黑洞吸积环（SDF 视界 + 连续内流吸入）+ 分层星星 + 环内流动微光
- **产出**：「音画黑洞」—— 光标即黑洞，环内静谧深空流动微光，环外星云持续被吸入；音频驱动整体亮度
- **挑战**：发布到 GitHub Pages / 打包成 Cocos Creator 应用

---

## 课程设计三条主线

1. **API 由浅入深**：绘制 → 数学 → 动画 → 数据 → 像素 → 物理 → GPU
2. **每个效果可独立运行**：每课一个 HTML，对照 COURSE.md 与代码学习
3. **最终汇合到「音画黑洞」**：从绘制基础一路到音频驱动的星云着色器大作业

## 课程形态：参数实验室 + 统一播放器

课程不是「代码 + 静态结果」，而是**玩参数**：

- 每个 API 的参数变成滑块/颜色/开关，拖动时画面**实时变化**，底部一行「活的代码」同步更新并闪烁高亮被改动的值。
- 「逐步拆解」模式：0→1 播放动画，画面被一步步「搭起来」，对应代码逐行出现，理解每个 API 的职责。
- 每课结尾有可选的「挑战」：调参数达成目标效果即通过。

### 参数实验室课程格式（`window.LESSON`）

| 字段 | 说明 |
|---|---|
| `no` / `title` / `subtitle` | 课号与标题，显示在播放器头部 |
| `width` / `height` | 画布分辨率 |
| `params` | 控件定义：`{ key, label, min, max, step, value }`，`type:'color'` 为取色器，`type:'bool'` 为开关，`type:'select'` 为下拉 |
| `code` | 代码行：`{ template, keys }`，`${key}` 会被实时替换为参数值；`show: (p)=>bool` 为条件行 |
| `steps` | 拆解节点：`{ at, label }`，`at` 为 0~1 进度 |
| `challenge` | 可选挑战：`{ desc, check(p) }` |
| `draw(ctx, p, t, step, mouse, sound)` | 每帧绘制：`p` 参数、`t` 秒、`step` 进度、`mouse{x,y,down}`、`sound{freq,level,playing}` |
| `liveStep` | `true` 时拆解模式不停动画 |
| `persist` | `true` 时画布由课程自管（拖尾用），播放器不清屏 |
| `audio` | `true` 时播放器显示「▶ 播放音乐」，向 `draw` 传 `sound` 频谱 |

### 播放器文件结构

| 文件 | 说明 |
|---|---|
| `player.html` | 播放器页面（画布 + 控件面板 + 代码条 + 挑战） |
| `player.js` | 引擎（控件渲染、代码同步、拆解动画、课程切换） |
| `lessons/NN-xxx.js` | 每课的 `window.LESSON` 配置，播放器通过 `player.html?lesson=NN-xxx.js` 加载 |
| `MANIFEST`（player.js 内） | 已上架课程的顺序，驱动上一课/下一课/下拉选择 |

### 当前状态

- **参数实验室**：01-35 **全部完成** 🎉（Canvas 2D → WebGL → 星云大作业）
  - 33 解剖重写 master 分支 starField，34 音频驱动，35 终极「音画黑洞」
  - 「完整代码」按钮可生成每课独立可运行的 HTML

## 跨阶段复用

| 可复用代码 | 出处 | 用在 |
|---|---|---|
| `heart.html` 心形参数方程 | 原仓库 | 06 课（已内置） |
| `index.html` 音频频谱 | 原仓库 | 13 课（已内置），33 课的音频源 |
| `starField.effect` 星云着色器 | master 分支（Cocos Creator） | 33-35 课解剖重写 |